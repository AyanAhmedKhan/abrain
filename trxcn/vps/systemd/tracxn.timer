[Unit]
Description=Run the Tracxn -> gbrain extractor on a schedule

[Timer]
# daily at 09:17 + up to 20 min jitter (avoid a round-number bot signature)
OnCalendar=*-*-* 09:17:00
RandomizedDelaySec=1200
Persistent=true

[Install]
WantedBy=timers.target
